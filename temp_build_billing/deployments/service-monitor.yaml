apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-billing
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-billing
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
