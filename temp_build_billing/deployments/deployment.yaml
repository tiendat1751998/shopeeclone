apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopee-billing
  labels:
    app: shopee-billing
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: shopee-billing
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: shopee-billing
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8099"
    spec:
      serviceAccountName: shopee-billing
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: shopee-billing
          image: shopee-clone/shopee-billing:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8099
            - name: grpc
              containerPort: 9099
          envFrom:
            - configMapRef:
                name: shopee-billing-config
            - secretRef:
                name: shopee-billing-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
