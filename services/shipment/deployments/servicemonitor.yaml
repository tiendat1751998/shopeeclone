apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-shipment
spec:
  selector:
    matchLabels:
      app: shopee-shipment
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
