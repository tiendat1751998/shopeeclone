apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopee-inventory
  labels:
    app: shopee-inventory
spec:
  replicas: 3
  selector:
    matchLabels:
      app: shopee-inventory
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: shopee-inventory
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8086"
    spec:
      serviceAccountName: shopee-inventory
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: shopee-inventory
          image: shopee-clone/shopee-inventory:latest
          ports:
            - name: http
              containerPort: 8086
            - name: grpc
              containerPort: 9096
          envFrom:
            - configMapRef:
                name: shopee-inventory-config
            - secretRef:
                name: shopee-inventory-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
      terminationGracePeriodSeconds: 60
