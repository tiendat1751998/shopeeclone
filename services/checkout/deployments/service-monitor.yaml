apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-checkout
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-checkout
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
