apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-order
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-order
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
