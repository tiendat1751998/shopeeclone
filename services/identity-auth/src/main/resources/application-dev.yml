server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tiki_auth
username: tiki
password: tiki_dev
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  access-secret: dev-access-secret-key-change-in-production
  refresh-secret: dev-refresh-secret-key-change-in-production
  access-ttl: 900000
  refresh-ttl: 604800000

logging:
  level:
    com.tiki: DEBUG
