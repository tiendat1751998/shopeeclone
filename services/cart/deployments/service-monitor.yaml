apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-cart
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-cart
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
