apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "shopee-auth.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "shopee-auth.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  minReadySeconds: 15
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      {{- include "shopee-auth.labels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "shopee-auth.labels" . | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      terminationGracePeriodSeconds: 60
      containers:
      - name: auth
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 8080
          name: http
        - containerPort: 9090
          name: grpc
        env:
        - name: APP_ENV
          value: {{ .Values.env.APP_ENV }}
        - name: MYSQL_HOST
          valueFrom:
            configMapKeyRef:
              name: {{ include "shopee-auth.fullname" . }}-config
              key: mysql_host
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "shopee-auth.fullname" . }}-secrets
              key: mysql_password
        - name: REDIS_ADDR
          valueFrom:
            configMapKeyRef:
              name: {{ include "shopee-auth.fullname" . }}-config
              key: redis_addr
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
