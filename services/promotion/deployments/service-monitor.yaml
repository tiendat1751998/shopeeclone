apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-promotion
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-promotion
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
