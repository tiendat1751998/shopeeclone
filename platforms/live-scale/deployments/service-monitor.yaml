apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-live-scale
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-live-scale
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
