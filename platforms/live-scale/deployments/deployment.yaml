apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopee-live-scale
  labels:
    app: shopee-live-scale
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: shopee-live-scale
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: shopee-live-scale
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8103"
    spec:
      serviceAccountName: shopee-live-scale
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: shopee-live-scale
          image: shopee-clone/shopee-live-scale:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8103
            - name: grpc
              containerPort: 9103
          envFrom:
            - configMapRef:
                name: shopee-live-scale-config
            - secretRef:
                name: shopee-live-scale-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
