apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-oms-fulfillment
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-oms-fulfillment
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
