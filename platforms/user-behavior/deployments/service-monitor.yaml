apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-user-behavior
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-user-behavior
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
