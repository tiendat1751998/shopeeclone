apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopee-developer
  labels:
    app: shopee-developer
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shopee-developer
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: shopee-developer
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8111"
    spec:
      serviceAccountName: shopee-developer
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: shopee-developer
          image: shopee-clone/shopee-developer:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8111
            - name: grpc
              containerPort: 9111
          envFrom:
            - configMapRef:
                name: shopee-developer-config
            - secretRef:
                name: shopee-developer-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
