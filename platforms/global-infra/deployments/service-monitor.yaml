apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-global-infra
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-global-infra
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
