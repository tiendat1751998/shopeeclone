apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopee-search-indexing
  labels:
    app: shopee-search-indexing
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: shopee-search-indexing
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: shopee-search-indexing
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8100"
    spec:
      serviceAccountName: shopee-search-indexing
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: shopee-search-indexing
          image: shopee-clone/shopee-search-indexing:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8100
            - name: grpc
              containerPort: 9100
          envFrom:
            - configMapRef:
                name: shopee-search-indexing-config
            - secretRef:
                name: shopee-search-indexing-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
