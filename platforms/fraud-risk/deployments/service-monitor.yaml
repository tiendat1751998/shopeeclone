apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-fraud-risk
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-fraud-risk
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
