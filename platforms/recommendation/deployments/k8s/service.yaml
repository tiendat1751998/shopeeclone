apiVersion: v1
kind: Service
metadata:
  name: shopee-recommendation
  labels:
    app: shopee-recommendation
spec:
  type: ClusterIP
  selector:
    app: shopee-recommendation
  ports:
  - name: http
    port: 8080
    targetPort: http
  - name: grpc
    port: 9090
    targetPort: grpc
