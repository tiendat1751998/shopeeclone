apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-recommendation
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-recommendation
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
