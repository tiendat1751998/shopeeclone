apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-analytics
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-analytics
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
