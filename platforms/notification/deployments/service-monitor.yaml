apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-notification
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-notification
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
