apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-notification-campaign
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-notification-campaign
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
