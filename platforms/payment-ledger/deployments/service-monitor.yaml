apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-payment-ledger
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-payment-ledger
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
