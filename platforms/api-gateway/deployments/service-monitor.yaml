apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-api-gateway
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-api-gateway
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
