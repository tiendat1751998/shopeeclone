apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-aiml
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-aiml
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
