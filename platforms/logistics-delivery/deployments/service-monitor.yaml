apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-logistics-delivery
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-logistics-delivery
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
