apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-advertising
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-advertising
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
