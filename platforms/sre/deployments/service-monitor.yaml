apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-sre
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-sre
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
