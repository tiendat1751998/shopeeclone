apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shopee-live-commerce
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shopee-live-commerce
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
