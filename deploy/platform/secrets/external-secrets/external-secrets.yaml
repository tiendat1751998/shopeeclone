apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: shopee-secret-store
  namespace: shopee
spec:
  provider:
    aws:
      service: SecretsManager
      region: ap-southeast-1
      auth:
        secretRef:
          accessKeyIDSecretRef:
            name: aws-secrets
            key: access-key
          secretAccessKeySecretRef:
            name: aws-secrets
            key: secret-access-key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: shopee-db-credentials
  namespace: shopee
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: shopee-secret-store
    kind: SecretStore
  target:
    name: db-credentials
  data:
    - secretKey: username
      remoteRef:
        key: /shopee/production/database
        property: username
    - secretKey: password
      remoteRef:
        key: /shopee/production/database
        property: password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: shopee-redis-credentials
  namespace: shopee
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: shopee-secret-store
    kind: SecretStore
  target:
    name: redis-credentials
  data:
    - secretKey: password
      remoteRef:
        key: /shopee/production/redis
        property: password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: shopee-kafka-credentials
  namespace: shopee
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: shopee-secret-store
    kind: SecretStore
  target:
    name: kafka-credentials
  data:
    - secretKey: username
      remoteRef:
        key: /shopee/production/kafka
        property: username
    - secretKey: password
      remoteRef:
        key: /shopee/production/kafka
        property: password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: shopee-jwt-credentials
  namespace: shopee
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: shopee-secret-store
    kind: SecretStore
  target:
    name: jwt-credentials
  data:
    - secretKey: jwt-secret
      remoteRef:
        key: /shopee/production/jwt
        property: secret
    - secretKey: jwt-public-key
      remoteRef:
        key: /shopee/production/jwt
        property: public-key
