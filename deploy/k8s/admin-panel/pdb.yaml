apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: admin-panel
  namespace: shopee
  labels:
    app: admin-panel
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: admin-panel
