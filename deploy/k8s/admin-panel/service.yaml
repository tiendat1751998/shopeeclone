apiVersion: v1
kind: Service
metadata:
  name: admin-panel
  namespace: shopee
  labels:
    app: admin-panel
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      name: http
      protocol: TCP
  selector:
    app: admin-panel
